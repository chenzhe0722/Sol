spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://database:3306/auth?characterEncoding=UTF-8"
    username: auth
  jpa:
    open-in-view: false
logging:
  level:
    root: error

server:
  port: 8443
  http2:
    enabled: true
  ssl:
    key-store: ${SSL_KEY_STORE}
    key-store-type: ${SSL_KEY_STORE_TYPE}
spring:
  r2dbc:
    url: "r2dbc:mariadb:gcomm://relation:3306"
    username: auth
    name: auth
  redis:
    cluster:
      nodes:
        - "cache:6379"
